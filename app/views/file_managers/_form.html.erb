<%= form_with(model: file_manager) do |form| %>
  <% if file_manager.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(file_manager.errors.count, "error") %> prohibited this file_manager from being saved:</h2>

      <ul>
        <% file_manager.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= form.label :file_name, style: "display: block" %>
    <%= form.text_field :file_name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content, rows: 10, class: "form-control" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
